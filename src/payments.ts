import axios from "axios";

async function creatOrder() {
    try {
        
    } catch (e) {
        console.log({
            type: e.type,
            message: e.message,
        });
    }
}

async function capturePayment() {
    try {
        
    } catch (e) {
        console.log({
            type: e.type,
            message: e.message,
        });
    }
}

export default { creatOrder, capturePayment }